# Copy to .env and set your own values. Never commit .env.

# PostgreSQL
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Local development (when running services outside Docker)
IAM_GRPC_URL=localhost:5001
CATALOG_GRPC_URL=localhost:5002
LOKI_URL=http://localhost:3100
